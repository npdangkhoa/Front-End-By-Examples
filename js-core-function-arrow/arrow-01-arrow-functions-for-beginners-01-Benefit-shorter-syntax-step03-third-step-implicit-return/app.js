/**
 * IMPLICIT RETURN Arrow functions allow you to have an implicit return: 
 * values are returned without having to use the return keyword.
 */
const app = (parm1, parm2) => ({'param01': parm1, 'param02': parm2})

app('01', '02');